# 2026-09-19 驗證紀錄

## 計分與資料

`node --test tests/*.test.js`：22 項全部通過，包含新增手動比分與同步時序回歸。

涵蓋三隊十二場、累計得分、負分限制、復原、31 分先到制、領先 2 分與封頂、完賽确认、單一進行場次、性別與人數配對、同分排名、賽制鎖定、Firestore 編解碼、開賽與當前勾選名單的原子儲存，以及專用 Firebase 設定／規則／私密檔排除檢查。

## 雲端整合

對專用 `courtside-badminton-20260919` 實際查核及驗證：

- Firestore `(default)` 位於 `asia-east1`，realtime updates enabled，刪除保護啟用。
- Web App、Email/Password 管理者及遠端規則已建立並部署。
- 管理者登入與寫入成功；公開觀眾讀取成功。
- 匿名寫入與不同 UID 寫入均由遠端規則拒絕（403）；測試用非管理帳號已移除。
- 兩個同版本競爭寫入只有一個成功，另一個不會覆蓋新版資料。
- 測試後還原正式賽事為全零、全部未開始、無测试播報。
- 實際瀏覽器取得「即時連線」及正式雲端比分，不是 localStorage 示範。

未修改既有工作用 Firebase project 或全域 Codex 設定，沒有把活動資料放入其他工作資料區，沒有啟用付費計費帳戶。

## 電腦與手機 UX

以瀏覽器 viewport 檢查 1440px、1280px、390px、320px；document scrollWidth 均等於 viewport width。這是響應式瀏覽器驗證，尚未涵蓋所有真實手機或 Safari 版本。

實際操作與修正：

- 手機先顯示目前比分、隊伍積分；480px 以下積分表只保留排名／隊伍／積分，360px 以下場次卡單欄。
- 點三角對戰，明細標題取得焦點並捲入畫面。
- 完整賽程移除重複的對戰切換列；場次詳情能返回當前戰況。
- 手機加減分按钮至少 56px 高；加分 18 → 19，再復原至 18，比分與累計值一致；同步重繪保留加分按鈕焦點。
- 錯誤上場組合在管理視窗內顯示提示；選擇球員後「確認名單並開賽」同時保存名單。
- 兩分頁測試：草稿存在時另一分頁加分，過期草稿停用儲存／開賽，按「載入最新名單」後可成功儲存。
- 隊員／賽制等儲存後，成功提示在視窗重繪後仍保留。
- 賽制採中文項目；僅顯示目前積分模式需要的設定欄位。
- 移除每次即時更新重播的入場動畫，減少畫面閃動。

## GitHub Pages 正式發布

- 獨立 repository：`Omnivorrjjjjjj/courtside-badminton`。
- GitHub Actions 完成測試與 Pages 部署；公開網址可載入所有模組並顯示「即時連線」。
- 在公開頁保持開啟時，以另一個已認證客戶端更新雲端播報，公開頁不重新整理即收到消息。
- 測試訊息還原後，公開頁亦即時回到無播報狀態；三隊積分均為 0。
- 公開網址的 390px 手機版無水平溢出，顯示比分、球員與三欄積分表。
- 管理密碼、管理者電子郵件、私鑰及 QA 檔案未納入 Git；正式規則拒絕匿名和非管理者寫入。

## 維運界線

這是小型三隊活動版本。仍需主辦者填寫正式隊名、姓名和活動名稱，並在實際球場網路測試使用中的手機。免費服務額度、第三方 SDK/CDN 與現場網路可影響可用性；斷線時保留最後取得的分數並停用寫入。

## LINE 分享標題修正

- 唯讀查得正式賽事名稱為「2026懷舊盃」；原始 HTML 仍為「三隊羽球對抗賽」，且缺少 Open Graph 標題。
- 原始 `<title>`、`og:title` 與描述現由專用 Firestore 的公開 title 欄位產生；每次 Pages 部署會重新讀取。未對賽事文件寫入。
- 初始載入畫面沿用已發布標題，避免雲端回應前短暫顯示預設賽名。
- 本次驗證為原始 HTML 與公開部署回應；未替使用者傳送 LINE 訊息，既有聊天預覽的快取狀態不在網站控制範圍內。

## 手動比分與連續更新修正

- 移除管理介面的逐球加減按鈕，改雙方比分輸入與單次更新；支援 Enter、修正比分、暫停中修正、整筆復原。
- 重現原錯誤：交易完成後 listener 尚未送達，本機仍停留舊 revision，下一次操作被誤判衝突。現改為交易確認成功後立即採用結果，再解除 pending；晚到舊 snapshot 不得覆蓋新資料。
- mock Firestore 回歸覆蓋連續提交先於 listener、舊 snapshot、真正版本衝突、其他播報更新、同場改分衝突、失敗後重試。
- 本機隔離示範實際操作：18：21 → 23：25 → 26：28，未重新整理；整筆復原回 23：25。
- 雙分頁驗證：另一視窗播報不會清除 24：27 草稿，且可正常送出；另一人改比分時出現載入最新提示；外部完賽後可重新開啟，不會因草稿卡住。
- 隔離測試副本人為延遲儲存四秒：送出 25：27 後，等待期間輸入 26：28；第一筆完成後第二筆內容仍保留且可直接送出。此為延遲模擬，並非宣稱現場網路速度。
- 390px 手機版輸入框與更新按鈕目視確認，無水平溢出；測試未改寫正式賽事比分或名單。
