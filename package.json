{
  "name": "courtside-badminton-live",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": { "test": "node --test tests/*.test.js", "start": "python3 -m http.server 4173 --bind 127.0.0.1 --directory dist" }
}
